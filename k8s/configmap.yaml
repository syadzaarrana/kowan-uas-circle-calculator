apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: circle-calc
data:
  PORT: "8080"
  NODE_ENV: "production"
  DB_HOST: "mysql-service.circle-calc.svc.cluster.local"
  DB_USER: "appuser"
  DB_NAME: "circle_calc"
  DB_PORT: "3306"
  SESSION_SECRET: "your-secret-key-change-this-in-production"
